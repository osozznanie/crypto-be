stages:
  - build
  - dockerize
  - deploy

build:
  stage: build
  image: maven:3.6.3-jdk-11
  script:
    - mvn clean package
  artifacts:
    paths:
      - target/

dockerize:
  stage: dockerize
  image: docker:19.03.12
  services:
    - docker:dind
  script:
    - docker build -t eureka-server:prod ./eureka-server/
    - docker build -t api-gateway:prod ./api-gateway/
    - docker build -t analytic-service:prod ./analytic-service/
    - docker build -t authentication-service:prod ./authentication-service/
    - docker build -t company-service:prod ./company-service/
    - docker build -t geography-service:prod ./geography-service/
    - docker build -t logging-service:prod ./logging-service/
    - docker build -t marketplace-service:prod ./marketplace-service/
    - docker build -t transaction-service:prod ./transaction-service/
    - docker build -t user-service:prod ./user-service/
  only:
    - dev

deploy:
  stage: deploy
  image: docker:19.03.12
  services:
    - docker:dind
  script:
    - docker-compose up -d
  only:
    - dev